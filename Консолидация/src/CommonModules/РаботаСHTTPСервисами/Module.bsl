
Функция ПолучитьСоединение(Логин, Пароль) Экспорт
	Хост = Константы.ИмяХоста.Получить();
	Возврат Новый HTTPСоединение(Хост,,Логин,Пароль);
КонецФункции	

Функция ПолучитьЗапрос(АдресРесурса) Экспорт
	Возврат Новый HTTPЗапрос(АдресРесурса);
КонецФункции	

Процедура ПроверитьКодСостояния(Код) Экспорт
	
	Если Код <> 200 Тогда
		ВызватьИсключение "Код состояния " + Строка(Код);
	КонецЕсли;	
	
КонецПроцедуры

